import keyMirror from 'fbjs/lib/keyMirror';

var AppConstants = keyMirror({
    GOTO_NEXT: null,
    GOTO_PREV: null,
    SINGLE_FRAME: null,
    DOUBLE_FRAME: null
  });

export default AppConstants;